{
  "schema": "zevryon.z3-css-materialization-window-foundation.v1",
  "milestone": "Z3",
  "status_change": false,
  "gate_progress": [
    "offscreen_materialization_bound"
  ],
  "admitted_gates": [],
  "production_surface": "materialize_css_style_window_v1",
  "resource_class": "ComputedStyle",
  "selection_model": [
    "caller supplies total logical document node count without a complete document terminal-style array",
    "caller supplies one bounded contiguous candidate terminal-ID window with an absolute document base ordinal",
    "visible range is an absolute half-open [visible_begin, visible_end) document range",
    "before and after lookahead are clipped at logical document boundaries",
    "candidate span must cover the clipped selected range and may not exceed the configured worst-case window",
    "selection takes one contiguous subspan of the bounded candidate and does not scan unrelated document nodes",
    "request mapping index i corresponds to selected_document_begin plus i"
  ],
  "hard_bounds": {
    "maximum_visible_nodes_limit": 65536,
    "maximum_offscreen_nodes_per_side_limit": 65536,
    "maximum_selection_work_units_limit": 65536
  },
  "certified_foundation": [
    "100000-node logical document requires only five candidate terminal IDs for a one-visible plus two-before plus two-after window",
    "candidate terminal input is bounded by maximum visible plus before plus after nodes and nested materializer request capacity",
    "leading and trailing lookahead clip without index underflow or overflow",
    "visible-node candidate-window and selection-work bounds fail before materialization",
    "nested materialization failures preserve prior output and map exact absolute document index",
    "wrapper failures preserve prior materialized output",
    "selection delegates only the computed contiguous candidate subspan to bounded materialization"
  ],
  "deliberate_exclusions": [
    "pixel geometry based viewport classification",
    "scroll velocity or time-based prefetch policy",
    "distance-ranked non-contiguous lookahead",
    "automatic DOM-to-terminal-style discovery",
    "cross-window materialization cache",
    "layout or paint object creation",
    "full offscreen_materialization_bound gate claim"
  ]
}
