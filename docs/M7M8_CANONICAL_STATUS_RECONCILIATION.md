# M7-M8 Canonical Status Reconciliation

## Purpose

This document reconciles the mainline plan with already-admitted M7 and M8 implementation authority. It does not claim the missing real physical/long-running evidence and does not convert CI smoke coverage into certification credit.

## M7 — implementation admitted, physical leadership evidence open

M7 competitor-laboratory implementation/admission machinery is canonical. Issue #126 remains open only because one real physical six-runtime evidence bundle, the frozen leadership evaluation, and publication have not yet been completed.

Canonical implementation state includes:

- frozen five lower-is-better core metrics and the 4-of-5 / remaining-within-5% leadership rule;
- common setup, warmup, query-timing and case-owned process-tree memory boundaries;
- warmup/execution semantics bound into the canonical scenario fingerprint;
- exact branded Chrome/Edge channels and exact Servo/Ladybird runtime identities without engine substitution;
- persistent Zevryon benchmark-session execution with implementation-local raw query timing;
- canonical six-browser x two-mode normalized collector;
- separate machine-readable five-metric leadership evaluator;
- exact-runtime preflight, stable preflight-to-measurement runtime identity and single-bundle/no-cherry-pick admission;
- physical-host/system-fingerprint/physical-Zevryon publication-manifest machinery;
- v2 raw-artifact admission replay and artifact-root containment.

Final v2 admission closure:

- PR #131 exact head: `e340f2cbe224493091d6feb40c45a41c7dee28ec`;
- single natural exact-head PR CI: `34094408179` — SUCCESS;
- canonical merge: `f998a6ae8df8aa4db41a15a9cd59b574f85c2480`;
- exact post-merge main CI: `34095489185` — SUCCESS.

The stale execution-plan wording that described this v2 slice as merely merge-conditioned must not be read as current state.

### Remaining M7 real evidence

1. Run the real six-runtime readiness preflight on the final physical benchmark host and preserve its M0 machine/thermal artifact.
2. Collect one complete canonical 6x2 browser evidence bundle on that same physical system without runtime substitution and with observed thermal evidence.
3. Collect Zevryon `virtualized` and `native-dom` physical cases with certified M0 receipts immediately before and after each case.
4. Admit all four raw artifacts through the v2 collection binder with artifact SHA-256 and stable runtime/host identities.
5. Evaluate the frozen five-metric leadership rule on that same admitted bundle.
6. Create the canonical v2 publication manifest only after artifact-root containment, re-hash/re-read, admission replay, clean Git commit/tree verification and receipt binding all pass.
7. Publish the complete admitted evidence and failure modes without best-of-N or cross-bundle metric mixing.

CI admission is not leadership evidence. Unsupported, unavailable, timeout, error or invalid runtime outcomes remain visible terminal evidence and prevent complete canonical coverage.

## M8 — implementation admitted, no-compensation evidence open

M8 implementation machinery is canonical through the provenance-bound final evidence binder v2. Issue #133 remains open because the real certification bundle has not been collected and executed to final PASS.

Canonical admitted implementation chain includes:

- deterministic storage crash-cut authority;
- fresh-process destructive storage crash authority, PR #134 / exact-head run `34107984010` SUCCESS;
- Titan giant-record, unbroken-token and pathological-grapheme dimensions bound into `score_100`, PR #135 / run `34112399277`;
- strict raw four-profile no-compensation evaluator, PR #136 / run `34114213421`;
- deterministic >=10M-capable mixed-mutation integrity authority, PR #137 / run `34115270075`;
- continuous dual-mode >=86,400-second-capable soak authority, PR #138 / run `34117234548`;
- Unicode/serializer/index/sequence property-fuzz authority, PR #139 / run `34118464587`;
- canonical full-envelope Titan fixture authority, PR #140 / exact head `a6746c64347252248ed8ea248224e57806469232` / run `34123284064`, merged as `d2ef419ff9c193499517539052a704e1affc60b1`;
- production profile runtime policy and exact four-case collection binder, PR #141 / exact head `b0992d7743c42a5e5716a2feeb18da2f8d50593e` / run `34127063247`, merged as `36e29b4c82a3049bc138fb3dda51531561967ed0`;
- physical profile case collector plus independent provenance verifier, PR #142 / fixed exact head `c5028fe5ffb9c8c03b5bb9df24323979bb929fe1` / run `34132583637` SUCCESS, merged as `e8383ca1b2aaafd43d9367997c95792dd507eb5f`;
- provenance-bound final evidence binder v2 and create-only frozen-bundle/import/raw recomputation stack, PR #143 / exact head `183eb00a45e92a3a3a4dcd5d7fc6b8eeca60495a` / run `34141947892` 7/7 SUCCESS, squash-merged as canonical `ba02e59f47cdbf5314d5eca4c51282aac9a78e68`;
- exact post-merge main CI on that final canonical binder head: `34158512860` — SUCCESS.

### Retained failed predecessors

M8 does not erase failed evidence:

- PR #142 first exact-head run `34131139757` exposed an ordinary-record UTF-8 boundary defect. The failed SHA was not rerun; the UTF-8-safe descendant above was admitted instead.
- final binder predecessor `3aa2e4bb604211064820baf7943baab850486bd0` / run `34140503331` remains retained failed evidence. It was not rerun unchanged; fixed descendant `183eb00a...` received its own natural run.

### Remaining M8 certification evidence

Implementation is not the blocker. One exact clean candidate/tree and one frozen bundle must still provide all of the following without compensation or cross-bundle substitution:

1. Generate and preserve the certification-mode canonical Titan corpus/report proving the full frozen envelope: 4 GiB logical UTF-8, 8,388,608 records, 67,108,864 logical nodes, 33,554,432 style runs, 1,048,576 resource references, 64 MiB largest record, 16 MiB unbroken token and 64 KiB pathological grapheme.
2. Collect exactly one independently verified physical attempt for each `legacy-phone`, `mid-phone`, `modern-phone` and `desktop`, all bound to the same candidate/Titan/probe identity.
3. Freeze one fresh external evidence bundle and import the four physical attempts through binder v2. A prepared `profile-observations.json` is not admissible physical evidence.
4. Execute the full fresh-process storage crash matrix into its create-only bundle slot.
5. Execute at least 10,000,000 certification-mode mixed mutations with deterministic integrity receipts.
6. Execute at least 10,000 certification-mode property-fuzz cases in each domain: Unicode, serializer, index and sequence.
7. Execute one continuous certification-mode dual-mode soak lasting at least 86,400 measured seconds after setup/warmup.
8. Run the final no-compensation binder exactly once on the complete frozen bundle.
9. Publish/archive the complete PASS or FAIL bundle with zero cross-bundle substitution.

Only exit status `0` from the final binder is M8 PASS. Exit `2` is valid evidence that fails at least one gate; exit `1` is invalid/inadmissible evidence. M7 physical leadership evidence is a separate boundary and cannot be manufactured by an M8 PASS.
