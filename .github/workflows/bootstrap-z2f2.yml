name: bootstrap-z2f2

on:
  pull_request:
    branches:
      - main

concurrency:
  group: bootstrap-z2f2-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/z2f-bounded-glyph-atlas-submission
          fetch-depth: 0
      - name: Materialize payload
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import base64, io, tarfile
          encoded = b''.join(path.read_bytes() for path in sorted(Path('.z2f2').glob('part*')))
          raw = bytearray(base64.b64decode(encoded, validate=True))
          assert len(raw) == 23_230
          assert raw[14_933] == 0x86
          raw[14_933] = 0xC6
          with tarfile.open(fileobj=io.BytesIO(raw), mode='r:gz') as archive:
              archive.extractall('.')
          path = Path('cmake/text_paint_command_stream.cmake')
          text = path.read_text(encoding='utf-8')
          include = 'include("${CMAKE_CURRENT_LIST_DIR}/glyph_atlas_submission.cmake")'
          if include not in text:
              path.write_text(text.rstrip() + '\n\n' + include + '\n', encoding='utf-8')
          PY
      - name: Verify expected payload
        run: |
          test -f src/glyph_atlas_submission.hpp
          test -f src/glyph_atlas_submission.cpp
          test -f src/glyph_atlas_submission_benchmark_main.cpp
          test -f tests/glyph_atlas_submission_tests.cpp
          test -f tests/glyph_atlas_equivalence_tests.cpp
          test -f cmake/glyph_atlas_submission.cmake
          test -f docs/Z2F2_BOUNDED_GLYPH_RASTER_ATLAS_SUBMISSION.md
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/glyph_atlas_submission.hpp src/glyph_atlas_submission.cpp src/glyph_atlas_submission_benchmark_main.cpp tests/glyph_atlas_submission_tests.cpp tests/glyph_atlas_equivalence_tests.cpp cmake/glyph_atlas_submission.cmake cmake/text_paint_command_stream.cmake docs/Z2F2_BOUNDED_GLYPH_RASTER_ATLAS_SUBMISSION.md
          git commit -m "Implement bounded glyph atlas submission"
          git push origin HEAD:work/z2f-bounded-glyph-atlas-submission
